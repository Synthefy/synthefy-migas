# Example config for scripts/latex/generate_table.py
# Paths are resolved relative to this config file. Use models: auto to infer from CSV columns.

context_groups:
  - context_length: 64
    # Relative to this file; or use absolute path
    csv_path: "../../results/suite/context_64/stats_Context_64_allsamples.csv"
    # Optional: restrict or rename datasets
    # datasets: [OIL_fred_with_text, SP500_fred_with_text]
    # dataset_display_names:
    #   OIL_fred_with_text: "Crude Oil"
    #   SP500_fred_with_text: "S&P 500"

# Use "auto" to infer models from CSV columns (*_mean_mae); or list explicitly
models: auto
# models:
#   - ttfm
#   - timeseries
#   - chronos_univar
#   - timesfm_univar

precision: 3
caption: "\\textbf{TTFM evaluation.} Mean MAE and Mean MSE on held-out datasets."
label: "tab:eval_results"
ttfm_cell_color_html: "EFEFEF"
